# Handoff — 2026-08-20

Written at the end of a long session, for whoever picks this up cold. Read
`docs/retros/2026-08-20-consult-the-artifact.md` first: it explains the one habit that
caused every failure this week, and the rest of this document assumes it.

## Where things stand

### Demo World wrap-up addendum

The Demo World foundation is now durable across both owning repositories:

- Comfy Quest `f480f5f` owns the importable Studio source, compiled experience,
  deterministic `demo-world-first-portal` `1.0.0` Runtime v2 package, and receipt
  contract. The create-only import race fix is `c3ac070`.
- `d0c27bf` introduced the low Arcane Sight breadcrumb path. `afc7ab8` is the r28
  Gallery builder that captures the existing lower/upper ascent pair before clearing
  and rebuilds at that absolute site; it does not use the player's incidental position
  when a complete Gallery already exists.
- Isolate `b14e2c1` publishes the accepted privacy-scanned canonical world pair and
  manifest. The final world is named `Comfy Quest Demo`, UID
  `-7600395338659582326`, and was provisioned successfully through the stopped-game
  public CLI with a valid external receipt. The exact tutorial package was delivered
  create-only while unrelated Runtime packs were preserved.

Live source-identity evidence proved exact activation, CHECK/CAST, targetless portal
completion, and the expected receipt chain twice. Derek's direct findings are now part
of the product contract: the target must say `CAST HERE`, be immediately visible from
hub arrival, use Valheim's fixed center crosshair, and explain that backtick only works
while F9 is open. The r28 composition uses three low Arcane Sight item-stand markers and
one restrained Social-colour ground torch; the upper ascent portal faces the target and
the birch obstruction is removed. Objective r28 evidence reported 1,916/1,916 loaded
objects at the reused site and passing ceiling clearance.

The final renamed world/UID was provisioned but not cold-loaded before the workstation
shutdown. Do not rebuild the Gallery or ask Derek to repeat the Charm lap. The next
acceptance step is one cold load and one human judgment: is the breadcrumb/sign path
immediately obvious? Record that result separately from the immutable canonical bytes.
The Studio import/edit/Play/rebind loop and a behaviorally clean replay after world reset
remain live-unverified; world reset deliberately preserves character and Runtime state.

Before any future automated live lap, take an install-wide ownership lock or equivalent
hash-set pin. Worktrees do not isolate the shared Valheim installation.

Landed on `main` today: `440ba0a` (W1–W4, both Phase 3 exit blockers plus content
profiles), `0ad6d08` (record correction), `55934ab` (Phase 4 scope packet), `e1aadb2`
and `f51fce0` (the audit), `c1279a4` (article relocation). Another agent landed the
Demo World foundation in parallel (`f480f5f`, `c3ac070`, `5d92ca0`).

Gates at time of writing: 294 python, 265 Lab xUnit, 85 Studio xUnit, Studio browser
E2E, 23-check harness self-test, boundary/identity/drift/repin/release self-tests,
gitleaks over 164 commits.

**Phase 3 still has not exited.** It needs lap session 3 and nothing else.

## The one constraint that bit twice today

**A git worktree isolates the repo. It does not isolate the Valheim install.** During
an armed lap, the other agent published into
`<Valheim>/BepInEx/config/comfy-quest-runtime/inbox/` (replacing the lap's quest) and
later deployed their own plugin build into `BepInEx/plugins/`. Both collisions were
silent. Cleanup correctly refused to restore over plugin bytes it had not placed.

Shutdown install state: the safety window is **closed**
(`PrivateWorldConfirmed = false`) and the installed Quest Lab plugin is the accepted
r28 build. The pre-lap originals remain preserved at
`captures/five-intent-slice1/phase3-exit-2026-08-20-s3/backup/plugins/`. The historical
copy moved to that run's `foreign-inbox/` can remain as evidence; Isolate provisioning
has since delivered the exact tutorial package to the production Runtime inbox, so no
manual hand-back is pending.

Before any future lap: agree who owns the install for its duration. The plan's proposed
gates (pin the inbox hash set at ValidateRevision; a run-id lock in the runtime root)
are not built yet.

## Next steps, in order

### 1. The top bar (the build Derek is waiting on)

His design, given in the seat on session 2 and unbuilt for a day because I turned it
into a scoping question. **It is not blocked.** ADR 0005's deferral clause is struck and
`docs/phase-4-scope-packet.md` carries a correction saying the composition is being
built, not scoped.

Scope: a horizontal top bar carrying the four-rung ladder, minimizing to four dots, with
one known player-configurable alert anchor. Design inputs gathered from a live
demo-world screenshot today:

- The ladder in its terminal state (all four green) spends a full row saying "nothing to
  do". That is the hundredth-run case and the argument for the minimize.
- The CAST strip's state (CHECK / READY / LANDED) is **not** derivable from the ladder,
  so it must survive the collapse as a second small indicator rather than a fifth rung.
- Warnings belong at the anchor, not in the evidence feed. The feed keeps history; the
  anchor carries "do this next". This is session 2's "if you didn't ask me to read it i
  wouldn't even have known it was there", made concrete.
- The interim deadline pill already uses a player-set anchor fraction
  (`Presentation/DeadlineAnchor`), which the real anchor should subsume rather than
  duplicate.

### 2. Stale warnings (small, found today, not yet fixed)

`RuntimeExperienceEngine.UnboundLine` fires once per activation and then sits in the
eight-row evidence ring. A live screenshot shows it reading "has no Charm yet" while the
card, Arcane Sight and the captures list all show a charm cast. A warning that no longer
applies actively misleads, because the taxonomy says a warning means act now.
Recommended fix: **warnings expire when the condition they describe clears** (preferred
over dimming them).

### 3. Lap session 3 (needs Derek's seat time, about fifteen minutes)

`docs/runbooks/OMEN-LAP-PHASE3-EXIT-S3.md` is now *derived* rather than written, from the
proven I2 beat order, a Studio rehearsal run, and the code's precondition chain. Its
human checklist is exactly what rehearsal declares it cannot prove: three verdicts.
`tests/test_quest_runtime_validation_lap.py` enforces beat ordering over every live
runbook, so the missing-step class of failure cannot recur silently.

Prep is scripted in that runbook. Two things learned today that it assumes:
`Get-DotNet9Executable` now resolves the toolchain from git's common dir so a worktree
works without `COMFY_QUEST_DOTNET`; and Studio's create/publish endpoints must be driven
with `curl`, not PowerShell 5.1's `Invoke-RestMethod`, which throws a client-side
`NullReferenceException` on POST with custom headers.

### 4. Scanner slice 1 (gated behind session 3 by the approved plan)

Schema only: `comfy-quest-world-scan/v1` in `ComfyQuestContracts`. Governing objective:
**a captured scan must be independently replayable as an observation corpus without
Valheim running**, with facts structurally distinguishable from later inference and
unknowns recorded as explicitly unknown. Six test properties are listed in the approved
plan at `C:\Users\derek\.claude\plans\zazzy-floating-yeti.md`.

Build it as a **repoint of `LabBlueprintBuilder.TrySelect`** (which already does a
bounded-radius ZDO walk with a 2048 cap and refuse-don't-truncate), not a new walker.
Two ADRs are planned and unwritten: a bounded world survey as a third observation scope,
and scan artifacts belonging to comfy-quest with MCP as isolate's transport. **Where** a
scan is written waits on the demo world's exchange-directory decision.

## Open decisions waiting on Derek

- **Phase 4 shape** — `docs/phase-4-scope-packet.md` asks three questions (which shape;
  does the top bar replace or coexist with the drawer; do the Lab ownership calls ride
  this phase). Recommendation is P4-C.
- **The article and post** — drafts are at `C:\work\writing\`
  (`product-already-told-us-linkedin-2026-08-20.md` and its `-post-` companion),
  following that workspace's house rules. Awaiting his edit and publish cue. I cannot
  post to LinkedIn.
- **Three cover-image prompts with filenames and captions** were given in conversation
  and are **not yet written to a file**. Derek's tested finding that image filename and
  caption carry roughly title-weight for reach in his network is recorded nowhere;
  `C:\work\writing\LI-PUBLISH-WORKBOOK.md` is where it belongs, with the sample-size
  caveat attached.

## Deliberately not done

Generalizing the three known instance-pinned tests (recorded in the retro with the rule
each should enforce); deleting the five-phase program structure (Derek's call); building
the install-collision gates; the semantic parser half of the scanner brief.

## Working state

Branch `worktree-scanner-slice1` at `.claude/worktrees/scanner-slice1`, rebased on
`origin/main` and pushed. `.claude/worktrees/youthful-tereshkova-c650fc` is an orphan
directory from an earlier session; git bookkeeping is already clean, delete after a
reboot.
