# Handoff — 2026-08-24

For whoever picks this up cold. Read this, then the reading order below. Everything here is
a pointer to something checked in; where this document and a cited surface disagree, the
cited surface wins and this document is stale.

Supersedes [`handoff-2026-08-20.md`](handoff-2026-08-20.md), which describes the program
before the 2026-08-24 adoption-path pivot.

## Before anything below: check your checkout

```
git -C <repo> fetch && git -C <repo> status -sb
```

If it is behind `origin/main`, everything below is stale and the ledger, the invariant and this
document may not exist on your disk at all. On 2026-08-25 the primary checkout sat nine commits
behind while a worktree pushed to `origin/main`, so an agent spawned there would have read a
roadmap that stopped being true nine commits earlier and had no way to notice.

## Why this exists

**Read `five-intent-program-plan.md` §"Program ethos and guardrails" — it is position 3 in the
reading order and it is the only thing here that says why any of the rest matters.** The program
is judged against the WeakAuras ecosystem ethos — small composable primitives, portable
artifacts, inspectability, immediate feedback, progressive complexity, community reuse — with
one inversion:

> **the machine absorbs the complexity that WeakAuras historically forced onto the human.**

And the thesis under it: *say, drop, pickup, wait are individually unimpressive;
`Say → within 10s Drop ×2 → Pickup → Equip → Consume → Heal` is an authored ritual. The
ecosystem effect is people combining constructs neither the system designers nor the original
pattern authors anticipated. Design for composition, not for impressive primitives.* The danger
named there is not missing capability — it is **exposing too much of it too quickly.**

Each lane in [`creator-os-phases.json`](creator-os-phases.json) then carries its own `question`
and `failure_mode`: 4A asks *can Creator OS operate a guild*, 4B *can I actually use it to build
one over time*, 4C *which repetitions have earned tooling*, 5 *can someone else receive,
inspect, and run what was built*. If a piece of work does not move one of those questions, it is
not on the path however green its gates are.

**What this repository does not hold.** The five source design intents live in the **baseline**
repository. `five-intent-program-plan.md` cites their immutable published revision and does not
restate them, so the original statement of intent is genuinely not here. Do not reconstruct it
from the plan; go read the source, or say plainly that you did not.

<!-- source-intents:begin -->
Pinned source authority: [`djcdevelopment/baseline@ed45c98fd5dfe8f92214fd67180937dc10163fee`](https://github.com/djcdevelopment/baseline/commit/ed45c98fd5dfe8f92214fd67180937dc10163fee).

| Intent | Immutable source | Bytes | SHA-256 |
| --- | --- | ---: | --- |
| 01 | [Arcane Sight observability](https://github.com/djcdevelopment/baseline/blob/ed45c98fd5dfe8f92214fd67180937dc10163fee/docs/arch/01_arcane_sight_runtime_observability.md) | 2771 | `025eddf14cec353b476938e682e75519629d208fd3040c54da55f7c7c163d610` |
| 02 | [Quest Lab apprenticeship](https://github.com/djcdevelopment/baseline/blob/ed45c98fd5dfe8f92214fd67180937dc10163fee/docs/arch/02_quest_lab_apprenticeship_spellbook.md) | 3089 | `8a6abcb6aa4e6a1b20aaf622f779773b9d628acf91f574714f42c52321c11ea5` |
| 03 | [Studio-Live closed loop](https://github.com/djcdevelopment/baseline/blob/ed45c98fd5dfe8f92214fd67180937dc10163fee/docs/arch/03_studio_live_valheim_creator_loop.md) | 3176 | `34ea44ce4009a534ecd212d78af83aed4c18cab96fdd3c39d2fdf358e6c328b4` |
| 04 | [Community artifact ecosystem](https://github.com/djcdevelopment/baseline/blob/ed45c98fd5dfe8f92214fd67180937dc10163fee/docs/arch/04_community_artifact_ecosystem.md) | 3481 | `84787fa8b99a978d6be0388b929f363c815f5da63047963f4949bc182761f807` |
| 05 | [Adaptive event semantics](https://github.com/djcdevelopment/baseline/blob/ed45c98fd5dfe8f92214fd67180937dc10163fee/docs/arch/05_adaptive_event_semantics.md) | 4015 | `6fafa4e79fed6af7dcac016c73670e2e580a0cd69cfabf85731ff805f90a7075` |
<!-- source-intents:end -->

## Where things stand

Lane 0 of the Creator OS build strategy is **closed**. Its job was narrow and it is done:

> untrustworthy roadmap + healthy implementation
> → **trustworthy roadmap + known implementation gaps**
> → implementation.

That second state is where you are now. The roadmap no longer misreports itself, and the
implementation gaps are named in a ledger rather than discovered one at a time.

The conclusion the whole program rests on still holds, and it is good news:

> **The remaining work is smaller than the roadmap makes it look, because the contract layer
> is consistently ahead of the runtime layer.**

`QuestPackStore.InspectLane` already validates, hashes, and compiles *N* experience documents.
Reset and rerun are wired end to end — endpoints, service, store, browser UI, file mailbox,
Runtime poller. The bounded request/receipt mailbox pattern exists in three tested flavours.
What is missing is usually one refusal, or live evidence, not a subsystem.

Read [`retros/2026-08-25-a-gate-you-walk-yourself.md`](retros/2026-08-25-a-gate-you-walk-yourself.md)
before trusting your own verification of anything here. Its one change is small and it is the
difference between a gate and a decoration: before reporting that a property is enforced, make
the negative case and watch it fail.

Three records constrain what you may quietly change:
[ADR 0015](adr/0015-the-requirements-ledger-cannot-confer-lane-authority.md) (the ledger records
lane authority and cannot confer it),
[ADR 0016](adr/0016-evidence-retention-archives-it-never-silently-deletes.md) (retention archives
the exact bytes and never destroys evidence without a receipt), and
[ADR 0017](adr/0017-an-absent-experience-selector-means-exactly-one.md) (an absent selector means
exactly one, and Runtime refuses rather than guessing).

## Reading order

<!-- reading-order:begin -->
1. `docs/PLAN.md` — **The plan.** Goal, where we are, what happens next, and what needs Derek. Read this alone and you know the program.
2. `docs/handoff-2026-08-24.md` — **Session record.** What the 2026-08-24/25 sessions did, the environment traps, and the cold-start checks.
3. `docs/five-intent-program-plan.md` — **Why this exists.** The ethos the whole program is judged against: six product guardrails and one communication guardrail.
4. `docs/creator-os-build-strategy.md` — **The plan.** Lane order, the program invariant, and what each lane may not do.
5. `docs/creator-os-phases.json` — **Lane vocabulary.** The only definition of a lane, and the human boundary.
6. `docs/creator-requirements-ledger.json` — **Requirement dispositions.** Who is accountable for each of the 47 requirements right now.
7. `docs/quest-mission-control.json` — **Work queue.** Every work item with its lane and requirement lineage.
8. `docs/creator-os.md` — **Operating strategy.** Why the loop is shaped this way, and the golden rule.
9. `docs/creator-portfolio-requirements.md` — **What is required.** The 47 FR-/NFR- requirements themselves.
10. `docs/adr/README.md` — **Decisions.** Append-only; a reversal needs a superseding record.
11. `docs/creator-os-audit-2026-08-24.md` — **Findings.** What was wrong on 2026-08-24, with `file:line` receipts.
<!-- reading-order:end -->

That list is generated from `docs/quest-mission-control.json` under `reading_order` and checked
on every render. A renamed authority, changed order, or unregistered projection fails the drift
gate. It is the fix for the reason this document exists — the previous handoff went stale and
the entry path still pointed at it.

## The next executable task

**Lane 4A is complete; the next product slice is `queue.guild-campaign` in 4B.** The committed
full-width journey proved the installed creator-to-Runtime mechanics, including reset, rerun,
retention, and restoration. It did not prove the corrected product hierarchy.

`queue.guild-campaign` is now `implemented`, not proven. Studio has the steward configuration,
immutable source, typed Signature Hunt abstraction, two derived artifacts, campaign, lineage
evidence, and a one-operation campaign-start path. That operation freezes exact bytes, prepares or
resumes the pinned session, arms Runtime, prepares the fixed fixture, publishes and verifies
activation, then
binds/starts the unique root through the fixture-owned sign. Its receipt ends at `started` and
explicitly says fixture preparation is not kill/completion proof and bind/start is not campaign
completion proof.

The simulated path now validates the exact fixture request and 17/17-object receipt, matches the
Deathsquito and Drake target tokens to exactly the two compiled instances, carries the prerequisite
machine/world/session pins through candidate and bind dispatch, requires the entry's explicit
successor, and validates the full applied binding reference. Campaign progression is editable on
every campaign and compiles successor flow from explicit prerequisite edges. These are
implementation facts, not installed proof.

The next executable task is to run that exact operation through real Valheim, complete both hunts
and the successor transition, retain terminal campaign evidence, reset/rerun, and close with owned
cleanup/restoration. No such live Signature Hunt proof exists yet. This is a narrow bridge, not
authority to build a generic abstraction framework, notebook, generator, rank engine, Discord
workflow, or world package.

`queue.workbench-boundary` is also technically ready, but remains unassigned pending the ruling
described below. It is not the selected product slice.

### Lane 2 landed 2026-08-25 — implemented, not proven

Audit C3 in both directions, from `NFR-OBS-001`.

- **Run-control receipts are partitioned by run.** They sat in one flat directory keyed by request
  id, and pruning ordered *all* of it by write time and deleted the tail — so a burst of resets on
  one run silently evicted an older run's receipts, the exact clause the requirement forbids. The
  cap is now 32 per run rather than 128 shared, against a registry that keeps up to 512 runs with
  predecessor lineage.
- **`RuntimeReceiptStore` has retention at all.** It had no cap, no prune, and `List()` enumerated
  and sorted every file on each call. Live window: 512 receipts or 30 days, swept every 64 writes
  so the game loop never pays per write.
- **Archiving moves the exact bytes.** A store read back as authority may not change the shape of
  its evidence at a retention boundary, so nothing is destroyed there — archived receipts are the
  same files in `receipts/archive/`, and `ListArchived` reads them.
- **The one place evidence is destroyed says so.** Past the archive bound, a
  `receipt_archive_evicted` receipt names the count and the time range. A gap in a correlated
  proof set is a fact you can read rather than something to infer from absence.
- `select_experience` receipts have no run, so they share a bounded `pack` partition. A scope may
  not escape its directory: run ids are `Safe()` identifiers and that policy admits `.` and `..`,
  which are safe as names and not as path segments.

Lane 2 had to land before Lane 4 collects the 4A evidence, or that evidence would be written to a
store that can drop members of a correlated proof set. That ordering is now satisfied.

## Open rulings — these need Derek, not an agent

Ten requirements are `parked`, which now has exactly one meaning: **still recognized and
relevant, but scheduling needs a ruling nobody has made.** Every one names its missing
decision in `pending_ruling`. The authoritative list is
[`creator-requirements-ledger.json`](creator-requirements-ledger.json); these are the ids, so
that this document cannot quietly fall behind it:

`FR-AUTH-001` · `FR-WORLD-001` · `FR-WORLD-002` · `FR-LOOP-002` · `FR-LOOP-003` ·
`FR-RUN-002` · `NFR-INTEGRITY-001` · `NFR-BOUND-001` · `NFR-MCP-001` · `NFR-TEST-002`

An agent may not resolve one of these by picking the obvious lane. That is the specific
failure the ledger's one-way lane authority exists to prevent: every lane it names points at
where the lane came from, and neither source is the ledger.

`queue.workbench-boundary` is the same ruling wearing a work item: state `ready`, lane
`unassigned`, with a `lane_note` naming the open question. Ready and unassigned at once is
information — technically executable, not selected as next work, pending a named ruling — not
a queue defect to normalize away.

## Deliberately left undone

Lane 0 repaired authority surfaces only. These are real, small, and tempting, and they were
**not** fixed on purpose — mixing them in would have destroyed the property that makes Lane 0
worth doing. Each carries a ledger entry naming the finding:

| Finding | What it is | Recorded under |
| --- | --- | --- |
| D2 | Action allowlist duplicated with different string comparers | audit D2 |
| D4 | Creator Session cannot drive run control | `FR-LOOP-002` |
| D5 | `NormalizeDocument` migrates v2 on read; import demands exact v3 | `FR-AUTH-001` |
| C5 | `MaxProjects` is reported and bounds import, but unchecked on create | `NFR-BOUND-001` |
| D6 / item 16 | Interim contracts package is version-pinned but content-mutable | `NFR-INTEGRITY-001` |
| item 18 | ~20 `Get-FileHash` call sites in nine scripts CI never runs | `NFR-INTEGRITY-001` |
| E2E | The Playwright suite is still ungated in CI | `NFR-TEST-001` |

Picking one of these up is fine. Doing it *while* doing something else is what is not.

## Environment gotchas that will cost you time

- **The interim package trap (D6).** `Comfy.Quest.Contracts.0.6.0-local.nupkg` was repacked
  without a version bump, so a warm NuGet cache serves stale contracts under the same version
  string. A plain `dotnet test src/Quest.Studio.Tests` fails with `CS0246` on
  `RuntimeRunControlReceipt` / `RuntimeRunStatusEntry` / `RuntimeRunStatusStore`. Set
  `NUGET_PACKAGES` to a directory keyed to the nupkg hash, the way
  `tools/quest-studio/Start-QuestStudio.ps1` already does. CI is unaffected — it restores clean.
  **Live instance:** the Lane 1 contracts change repacked that package again without a version
  bump, because bumping it is punch item 16 and item 16 is parked. A warm cache will therefore
  compile Studio against contracts with no `ActiveSet.ExperienceId` and fail on the selector.
- Host `dotnet` is 8.0; .NET 9 lives at `C:\work\dotnet9\dotnet.exe`. `Quest.Studio*` targets
  net9.0.
- The Creator Session tests spawn real `powershell.exe` (5.1). No `&&`, and
  `Get-Content`/`Set-Content` round trips corrupt UTF-8. `Get-FileHash` is unresolvable on the
  hosted runner for reasons outside this repository — hash through
  `System.Security.Cryptography.SHA256` instead.
- **A worktree isolates the repository, not the game install.** Agree who owns
  `<Valheim>/BepInEx/` before any lap. Two separate incidents came from not doing this.

## Verification

```
python tools/render_quest_mission_control.py --check
python -m unittest discover -s tests
python tools/assert_no_reach_in.py
```

The drift gate is not advisory. Adding or removing a requirement in
`docs/creator-portfolio-requirements.md` without a matching ledger entry fails CI, and so does
a work item with no lane, a requirement with no disposition, and a reading-order pointer that
no longer matches. That is deliberate, not friction to route around.
