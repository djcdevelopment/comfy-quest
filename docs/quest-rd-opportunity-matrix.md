# Quest toolkit R&D opportunity matrix

This is a lap-selection aid, not a product backlog. The working rule is to prove the
smallest edge that unlocks a larger authoring surface, project it through Grimoire,
Studio, Runtime, and receipts, then use one short game batch to decide what deserves
another cycle.

## Current projection

`Grimoire primary seam -> generated fast-signal catalog -> Studio beat -> Runtime adapter -> progress receipt`

The catalog is intentionally small. Say, shout, drop, pickup, equip, consume, heal,
and durable wait are enough to explore ordering, repetition, time windows, feedback,
and rewards without asking an author to identify ZDOs or manipulate graph plumbing.

| Edge | What is structurally projected | Remaining live question |
| --- | --- | --- |
| Say / shout | privacy-minimal chat mode, fixed target | already proven locally; multiplayer actor changes are a separate lap |
| Drop | wildcard stable item prefab | already observed live; exact-item authoring can wait |
| Pickup / equip / consume | reviewed `core` + `primary` Grimoire seams and local-player Runtime adapters | confirm all three once in the next OMEN batch |
| Heal | wildcard local healing signal | already observed; decide later whether amount thresholds are worth exposing |
| Wait | durable named timer | already proven; keep time controls simple |
| Repeat + window | contract `COUNT(EVENT)` with visible partial progress | confirm `1/2` then completion in one live lap |
| Resume Creator Session by id | install-local session manifest carries the exact machine, world, profile, and recovery pins | passed on the active ERA17 session: `Status -SessionId era17-broken-way-20260827-r2` resolved OMEN, world `-523956327`, `ComfyEra17`, `tugcorp`, matching plugin hashes, running game, current 0.1.1 activation, and stage `start` without requiring redundant machine/world arguments |
| Open a newly created guild for editing | Studio persists and selects the guild, but its settings remain inside a collapsed disclosure | passed in Broken Way authoring r5; the visible disclosure was opened and `guild-2de197289aea` now holds the authored title, premise, three bands, questline, six ordered quests, and two repeatable events |
| Cross from the library into authoring | a new blank draft opens while the library overlay remains an intentional modal boundary | passed in r5 by closing the visible library boundary before author-canvas input; all eight campaign projects were authored through Studio |
| Wait for the new draft identity | project creation is asynchronous, so the previously open clean draft remains a valid page until the new project response is loaded | passed in r5; the unrelated `desperate-defense-c2afbc` title was restored and eight distinct campaign project/experience identities were retained without another cross-draft edit |
| Place an artifact in a progression band | Studio derives the available band choices from the selected guild destination | passed in r5; six quests and two repeatable events are placed in the intended Find/Mend/Keep bands and the compiled guild contains eight experience documents |
| Resume the already-current draft | selecting the current project can legitimately leave the identity predicate true before the drawer transition finishes | passed in r5; current-project resume no longer raced the drawer and the full author/rehearse/publish preparation completed |
| Consolidate bounded Runtime control through Isolate | a product-owned provider release contributes status, receipts, identity-pinned creator requests, and bounded run control to an explicit Isolate profile | installed slices passed the Quest path: a missing caller credential and wrong ERA17 world/session identities were rejected, the correct session armed, 32 bounded candidates were listed, the selected experience rebound, and the exact 0.1.1 run reported. Final r6 status returned its own non-secret Creator Session identity, and a follow-up control succeeded using only that bounded status result; released identity, per-lap generated credentials, volunteer-machine setup, and teardown remain open |
| Teach exact buildable piece targets | Studio examples come from the generated creator catalog and Runtime compares the prefab string emitted by Valheim | installed Valheim 0.221.12 and the matching prefab dump proved `woodwall`, not `wood_wall`; the four examples, both campaign routes, generated catalogs, and live Studio were corrected, and a mutated `wood_wall` example now turns the generator test red |
| Relate a Studio project to its containing guild release | Studio compiles the current guild identity, filters receipts to the selected experience, and recognizes explicit Isolate binding receipts | source integration tests and the installed ERA17 screen now report The Broken Sign as `current` / `bound` with matching 0.1.1 hashes and Validation, Transfer, Activation, and Rebind all PASS; a sibling project reports `other_experience` instead of the false `other_version` |
| Tolerate a briefly stalled armed Runtime | Play distinguishes a missing/disarmed Runtime from an already-armed heartbeat that has gone temporarily stale | the installed pre-fix sequence rejected once and succeeded after the Valheim heartbeat resumed; r5 then showed Studio's run view fail closed during a real Valheim main-thread pause and reconnect on the next heartbeat. The patched Play path waits up to 30 seconds only for an already-armed status and its delayed-heartbeat integration test passes; the exact installed Play grace branch remains open |
| Bind control authority to completed world entry | Runtime latches a Creator Session only after exact world entry reaches `entered`, and both creator and run-control receipts echo that identity | passed in installed r5: creator request `mcp-creator-status-20260827T153152128008Z-bfe24e02` rejected r4 with `creator_session_mismatch`, run request `mcp-run-list_binding_candidates-20260827T153157142232Z-b67f3790` rejected it with `runtime_creator_session_mismatch`, and only r5 armed and returned 32 candidates; source tests also reject missing and forged session evidence |
| Bound synchronous provider calls | each request expires before its tool wait; after timeout the provider leaves the fixed mailbox for Runtime or Lab to claim and reject instead of racing a successor through compare-and-delete | focused provider tests cover expiry and fail-closed mailbox occupancy; two consecutive package builds produced the same archive hash. The in-process lock serializes callers in one provider process; a released Isolate profile still needs its own single-provider lifecycle rather than treating that lock as a host-wide lease. |
| Let a large world finish saving | Creator Session shutdown waits for Valheim's process exit, with a two-minute minimum before force, and preserves any interrupted `.new` file in evidence | passed in installed r5: r4 exposed the old 20-second kill and its 496 MB partial was quarantined; the patched Stop then let ERA17 spend 32.46 seconds writing its 1.30 GB database, observed `World saved`, exited gracefully with no forced process, and left no world or character `.new` files |
| Cold-resume a large-world Creator Session | the retained session manifest is enough to select the exact machine, world, profile, and deployment without transcript context | passed after the installed r6 edge: the documented default first reported failure at 60 seconds while ERA17 was legitimately loading 8,921,562 ZDOs, then the narrow fix made Launch retain the driver's proven 780-second world-entry bound unless the caller explicitly overrides it. A no-override replay entered the exact world/profile at 00:45:36Z and recorded `world_entry_wait_seconds: 780`. Exact rerun: `tools\creator-session\Invoke-CreatorSession.ps1 Launch -SessionId era17-broken-way-20260827-r6`. |
| Pin unattended entry to the Steam account that owns the character | Prepare must select the pinned profile only from the active Steam account's save inventory and retain that account identity through Launch | r8 exposed the cross-account scan: Prepare found `tugcorp` under account `128445914`, then Runtime correctly rejected it when Valheim ran as account `15896021`. The narrow guard now resolves Steam `ActiveProcess\ActiveUser`, restricts cloud discovery to that userdata root, records the account in the session manifest, and refuses account drift before Launch. r9 Prepare pinned persona `wary.fool` / `128445914`, selected that account's `tugcorp.fch`, and both launches entered exact world `-523956327`. Exact rerun: `& .\tools\creator-session\Invoke-CreatorSession.ps1 Launch -SessionId era17-pilgrimage-20260827-r9`. Remaining boundary: changing Steam credentials/accounts is deliberately human-owned. |
| Submit strict run control without transporting freshness through the agent window | the Quest provider owns one bounded mailbox request and its correlated receipt; Studio's three-second status gate remains a UI safety precondition rather than a manual timing ritual | r9's split Studio API attempt returned HTTP 400 before dispatch and lost its typed body. The next lap used the packaged provider path: request `mcp-run-list_binding_candidates-20260828T044340653255Z-5eb9d648` returned one correlated completed Runtime receipt with 32 candidates, and bind request `mcp-run-bind_selected_experience-20260828T044354036319Z-ea968d38` selected the nearest persistent sign. Exact rerun: invoke `comfy_quest_workbench.quest_runtime_run_control(operation='list_binding_candidates', expected_machine='OMEN', expected_world_uid='-523956327', creator_session_id='era17-pilgrimage-20260827-r9')`. Uncertainty: the lost Studio error body remains unclassified; it is not needed for the provider-owned slice. |
| Ride through an atomic Runtime heartbeat replacement without weakening the gate | Studio opens the status snapshot before inspecting its length and retries only FileNotFound/DirectoryNotFound/IO replacement windows five times at 10 ms; JSON, schema, identity, freshness, and bounded-shape failures remain immediate or fail closed | the complete browser suite repeatedly exposed pre-dispatch HTTP 400 `runtime_run_status_unreadable` at reset and restore while each test passed alone. Failed artifacts `20260828T092628199Z-3baef4b1f0e2430491625c94a0570727` and `20260828T092745124Z-6ce895b7b3af49eab84cf9ec01536b3f` contained no apply mailbox or receipt. The synthetic Runtime was corrected from 25 Hz duplicate replacement to the installed 1 Hz cadence and now publishes status/evidence before its scoped receipt commit marker. Locked-file recovery and exhausted-window/no-mailbox regressions pass; the combined suite passes two synthetic journeys with one installed-game opt-in skip. |
| Carry durable binding identity through Isolate status | a cold caller must correlate the logical Charm binding without reading Runtime's filesystem or assuming its physical ZDO stayed stable | passed in r9 with provider archive `ac383971402302b3bf55b75300d67dab05c1bc6bf427e231cbbac982ffb1d7b8`. Before save, provider status reported run `run-20260828T044359704Z-e5dc6393`, scope `scope-36a0c31b4020e272a702df76f0de3e53`, marker `binding-20260828T044359683Z-fc3f2842`, experience `quest-0d3372`, hash `6e0f85…3526e`, and physical ZDO `1:3177522`. After a 57-second graceful save and cold entry, the first read-only provider heartbeat preserved every logical field and reported physical ZDO `1:3177521`. No Arm, bind, or selection request occurred between entry and the post-cold read. Exact rerun: Stop, Launch r9, then call the packaged `quest_runtime_status(max_runs=32)` before any mutation. Uncertainty: duplicate markers beyond Runtime's loaded-object set and the restore-to-empty crash window remain unsampled. |
| Preserve the reviewed binding across cold world entry | a retained creator seat must reopen on the same logical world object, and Studio readiness must describe the fresh run rather than historical binding evidence | passed in r9 under the durable marker: the saved sign reloaded with a different physical ZDO (`1:3177522` -> `1:3177521`) while run ID, scope, marker, experience, content hash, stage, and participant stayed unchanged. A fresh Studio read then reported `connected: true`, the new physical ZDO, the same marker/run/scope, phase `bound`, relation `current`, title `The Name in Ash`, and four PASS lines whose Rebind correlation is the current marker. Exact rerun: read project runs and Runtime status immediately after the post-cold provider heartbeat, before any control request. Uncertainties: spawned-object cleanup still keys its ledger by physical ZDO and is a separate unsampled lap; global restore uniqueness remains bounded by candidate enumeration. |
| Retain an explicit experience choice across a guild hot revision | activation may carry a selector only within the same pack and only when the new archive resolves that exact experience once; removed, duplicated, unreadable, and cross-pack choices still clear | r11 activated guild 0.1.2 but rejected `dev_rebind` with `active_experience_ambiguous` because the eight-experience guild activation cleared `quest-0d3372` before the durable Charm could be updated. The narrow fix then passed in r12: revision 29 / 0.1.3 retained the exact selector, activated hash `197bd600…b702`, rebound only marker `binding-20260828T044359683Z-fc3f2842`, skipped a historical anchor as `superseded_binding`, created run `run-20260828T054222875Z-f3bc2ff5` / scope `scope-00e2758c59269d64eda0e82c3d625226`, and advanced `experience_started` to `name-in-ash` without another select, bind, or move request. Exact rerun: r12 `Prepare -> Launch -> Arm`, then Studio Play guild revision 29 and read packaged provider status. |
| Restore a dense world's declared connection support after install cleanup | Creator Session resolves a support manifest by exact world UID, pins the external bytes and sanitized config, deploys and restores them with the session, and requires current-launch cache signals while excluding vanilla scan/error markers | r12 cleanup removed NetworkSense's connection caches along with other non-Quest add-ons; Derek then reported a distracting hard freeze about every three seconds during an otherwise "pretty inspiring" play-through to `pilgrim-choice`. Corrected r13 Launch recorded 5,171 cached saved portal links and a spawner pass over 92,359 sources / 19,493 targets, with no vanilla markers. A separate artifact sampled recurring full-world cache cycles at 23:27:15, 23:28:15, and 23:29:15 over 13,875 portal objects with zero dirty work, while Runtime remained armed at `pilgrim-choice`. Exact rerun: `& .\tools\creator-session\Invoke-CreatorSession.ps1 Launch -SessionId era17-pilgrimage-20260827-r13`. Remaining live question: Derek must confirm whether the perceived freeze is gone. Limits: the support DLL is unsigned captured external code, registers its full patch graph, and supports only the current static topology because its post-load tag index is stale; portal traversal, creation, and retagging were not sampled. |
| Capture proof from BepInEx while it still owns the live log | Launch reads and copies the open log through a `FileShare.ReadWrite | FileShare.Delete` handle, validates the exact captured copy, and hashes only after validation | the first r13 game launch entered successfully and emitted every cache signal, but `[IO.File]::ReadAllText` requested incompatible sharing; the caught failure reset the sample to empty text, so Launch rejected all signals and wrote no successful operation. The corrected cold replay produced `20260828T062556Z-launch\operation.json` SHA-256 `0e0b62854de5f418e66526eeec8102256ec5fbffd6063dfb5185f98692a7cb85` and support-log SHA-256 `ec8035c300777c3307b519228d94f64ce7bd4b9cc1e6edd46562d7e793bde615`. This is execution proof, not a test claim. |
| Route mechanics through the small installed world | ComfyQuestDemo is the default Creator Session pair and the destructive full guild journey refuses any other world before Prepare | ERA17's 1.30 GB database made routine iteration pay acceptance-world load/save and support costs. The pivot closed r13 without game-state rollback, quarantined its interrupted `.db.new`, then prepared `quest-fastlane-20260828-r1` on the 3.1 MB ComfyQuestDemo / questyfour pair under the same Steam account. Cold entry completed in about 24 seconds with four Quest assemblies, `world_support: null`, no NetworkSense or loader failure, and Runtime Arm completed. The world-routing guard was then invoked against ERA17 and observed refusing before install mutation. Exact active-session check: `& .\tools\creator-session\Invoke-CreatorSession.ps1 Status -SessionId quest-fastlane-20260828-r1`. Remaining boundary: immutable content/package bytes promote between worlds; bindings, runs, and receipts do not. |
| Keep every binding target kind visible inside the bounded candidate wire | candidate selection reserves the nearest locally owned object of each supported target kind, then fills the remaining 32 slots by deterministic distance order | AM4 cold execution showed why the narrow fix and the root cause must remain separate. The patched scan still had no sign, disproving crowding as the immediate cause; after the reviewed Demo fixture was replayed, the same bounded scan returned sign `631591890:63831` at 5.6 m alongside dense generic pieces. Exact rerun: call packaged `quest_runtime_run_control(operation='list_binding_candidates', expected_machine='am4', expected_world_uid='-7600395338659582326', creator_session_id='quest-am4-fastlane-20260828-r1')` after a fixture `MATCH`. Uncertainty: more supported target kinds than the wire maximum and equal-key duplicate ordering were not sampled live. |
| Replay a reviewed Godbuild on Linux before binding discovery | one provider call hash-verifies the reviewed manifest/artifacts, reasserts live private-world Runtime authority, and performs ground-only `blueprint_check -> blueprint_count -> optional blueprint_build -> blueprint_diff`, then restores authority it enabled | the first AM4 check exposed and fixed Mono's `DataContractJsonSerializer` failure. Provider archive `b4ec7d4a...020c` then executed the complete precondition target-locally: first run built 12 pieces and returned exact `MATCH`; immediate retry omitted `blueprint_build` and returned `blueprint_replay_already_match`; every run verified `build_off`. After a deliberate clear, the same build/no-op pair passed again. Exact evidence index: `docs/evidence/quest-am4-fastlane-20260828-r1.json`. Remaining boundary: the clean identity-attested Isolate profile, per-lap credential, volunteer-machine setup, and correlated teardown are still open. |
| Wait for Valheim to retire cleared objects before declaring success | `blueprint_clear` returns only after the mark-scoped standing count reaches zero, within a bounded 120-frame wait | live request `provider-clear-94641fa2284c4cfc9176af6634b173d9` removed 12 pieces but returned `failed` because same-frame validation still saw their retiring ZDOs. After the coroutine correction and cold Lab deploy, request `provider-clear-fixed-20260828T091833Z-9f43d8e1` returned `completed`; the provider then rebuilt all 12 and its next retry was a no-op exact match. |
| Make a reset complete only after its exact successor has started | reset persistence retains one successor identity across retry, and Runtime idempotently supplies that exact run's `experience_started` event until its workflow exists | AM4 bound `quest-0d3372` to durable marker `binding-20260828T082859963Z-05d8607d`, reset run `run-20260828T082859977Z-eacae6c4`, and created only successor `run-20260828T083011921Z-c1818db2`. The successor independently advanced to `name-in-ash` on the same scope and binding. Replaying the same confirmed reset returned reset `reset-f4e521cf134f4a3581775556f18bce5f` and the same successor with no additional event/action/transition chain. Exact evidence index: `docs/evidence/quest-am4-fastlane-20260828-r1.json`. Uncertainty: the injected successor-start failure/recovery branch is source-tested but was not deliberately induced in the installed client. |
| Put visible machine-only Valheim work on an unattended fleet seat | AM4 carries the game window, Steam lifecycle, mailbox traffic, and evidence capture while OMEN remains available for Derek's ordinary work | the AM4 lap launched, entered, replayed, bound, reset, retried, and captured evidence without Derek touching either game UI; Steam persona `Zephar410` and the exact Demo identity were observed in the client log. This removed focus stealing and accidental tabbing from the workflow. Remaining boundary: process lifecycle is still an Isolate/host responsibility, not a Quest-provider feature, and a later ERA17 sitting returns to OMEN only for perception and authored choice. |

## One prepared batch lap

Use Studio's **R&D Signal Circuit** template. Rehearse it in the browser, publish one
immutable iteration, then do the explicit Runtime Check and Load. The game portion is
one sequence on OMEN: say, wait, shout, drop twice, pick up, equip, consume, and heal.
The Runtime cockpit should name the current beat and show partial count progress. Save
the resulting receipt set as the decision evidence.

This lap does not need i5. Use the second machine only when the question is peer role,
listen-host authority, replication, or fail-closed multiplayer mutation. Content,
ordering, messages, rewards, and browser UX stay in the local Studio/rehearsal loop.

## Decision after the batch

Promote only edges that are both useful and legible in receipts. The likely next
cycle is one of:

- item specificity, if wildcard pickup/equip/consume is too vague;
- simple thresholds, if count/window and healing amount create useful rituals;
- compact alternatives, if authors need optional beats more than more signals;
- additional Grimoire `core` seams, selected from observed quest ideas rather than
  exposed wholesale.

Keep exact-world targeting, arbitrary branching, generalized variables, and new
multiplayer authority paths out of the fast lane until a concrete quest needs them.
They remain available as Advanced work, but they should not tax the default R&D lap.
